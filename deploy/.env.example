# 复制为 .env 并修改
DB_PASSWORD=aom
JWT_SECRET=change-me-in-prod
ADMIN_INIT_PASSWORD=admin123
# 飞书服务台交接链接的公网访问根地址（本地 ngrok / 反向代理环境必须填写）
ITOM_PUBLIC_URL=https://your-public-host.example.com
# 全新数据库初始化六条流程及当前登录页/Logo；已有品牌配置不会覆盖
SEED_INITIAL_CONFIG=1
