# ITOM 产品需求文档（PRD）

> 版本：v1.1（2026-07-19，同步 M34–M37 实现）
> 上游依据：[01-系统改造建议.md](01-系统改造建议.md)、[02-字段裁剪清单.md](02-字段裁剪清单.md)
> 本文档自成一体，是后续技术设计、开发与逐里程碑验收的唯一基准。

---

## 1. 产品概述

### 1.1 产品定位

ITOM 是一套面向 IT 团队的**轻量运营管理平台**，覆盖六个域：总览、ITSM 服务管理、项目管理、需求管理、流程引擎、团队管理。

### 1.2 设计原则（全系统强制约束）

1. **极简录入**：任何创建表单必填项 ≤ 8（实际全部做到 ≤ 5）；进阶字段默认折叠；与生命周期后期相关的字段在对应阶段才出现。
2. **派生数据零录入**：一切可计算的数据（编号、SLA、进度、健康度、成本汇总、积分、排行、达成率）由系统自动生成，页面上没有录入入口。
3. **渐进披露**：表单只在需要时展开（如工单选"变更"类型才出现变更字段）。
4. **事件驱动**：关键动作统一产生领域事件，驱动通知（预留飞书）与积分。
5. **无预计算表**：所有统计实时聚合，不维护任何月度统计/快照表

### 1.3 不做什么（明确排除）

- 组合治理独立域（阶段门/收益实现/能力地图/路线图）——组合仅作为项目的分组标签
- 数据治理域、组织成熟度评估、OCM
- ITSM 持续改进（CSI）、知识评审流、CAB 独立评审装置、ISO27001 安全事件字段
- 需求的版本快照、多人审批流、调研会话、三维打分
- 项目的干系人矩阵、基线管理、质量指标页、完整 EVM 录入
- AI Agent 注册/编排/升级装置、n8n 外部工作流引擎（流程能力内置实现，事件出口预留挂接点，见第 8 章）。飞书组织同步、扫码认证、工作台免登及账号绑定已在 M11/M36–M37 实现，不再属于排除范围。
- 数据迁移（空库启动）

---

## 2. 用户与权限

### 2.1 角色（v1.0 初版定义，最新定稿见 2.1a：16 个内置角色）

| 角色 | 说明 | 典型用户 |
| --- | --- | --- |
| admin 系统管理员 | 全部功能 + 系统配置（用户、状态机、SLA 策略、团队活动积分规则、流程定义） | 平台负责人 |
| it_pdm IT 产品经理 | 团队成员基础权限；侧重需求域：需求分析/排期/验收/关闭 | 产品经理/BA |
| it_pm IT 项目经理 | 团队成员基础权限；侧重项目域：项目创建与管理、WBS/里程碑/风险/成本维护 | 项目经理 |
| it_dev IT 开发 | 团队成员基础权限；侧重交付：需求开发任务、WBS 任务、工单处理 | 开发工程师 |
| it_ops IT 运维 | 团队成员基础权限；侧重运维：工单/变更实施/问题分析、CMDB 与 SLA 维护 | 运维工程师 |
| is_mgr 信息安全管理员 | 团队成员基础权限；侧重安全治理：安全类工单/问题处置、安全类 CI 维护、变更复盘与安全审计、审计日志查看 | 安全管理员 |
| it_bp IT 业务合作伙伴 | 团队成员基础权限；侧重业务接口：需求登记与业务对齐、代业务部门提单、验收协同、满意度跟进 | IT BP |
| requester 业务用户 | 提交工单和需求、查询自己提交的单据、评价满意度 | 业务部门同事 |

> it_pdm / it_pm / it_dev / it_ops / is_mgr / it_bp 六个岗位角色统称"团队成员"，基础权限一致（处理指派给自己的单据、录入知识/培训/建言、查看 Dashboard 与自己的积分），差异体现在两处：**流程引擎步骤的默认指派角色**（见第 8 章）和登录后的默认菜单侧重。一人可兼多角色（如既是 it_dev 又是 it_tm）。角色代码注意：`it_pdm` 为产品经理，`it_pm` 为项目经理。

### 2.1a 矩阵式组织角色（M3.6，2026-07-11 增补，详见 docs/06 §六/§七）

- 内置角色定稿 **16 个**：包含 `admin`、`cio`、`it_bm`、`it_tm`、`it_pdm`、`it_pdm_leader`、`it_pm`、`it_pmo`、`it_dev`、`it_dev_leader`、`it_ops`、`it_op_leader`、`is_mgr`、`it_bp`、`auditor`、`requester`。**内置角色名称/描述可编辑**（代码/继承/删除锁定）。
- **功能权限矩阵**：系统模块 × 4 动作（可见/新建/修改/删除）按角色配置（系统管理→权限配置页）；admin 隐式全权不可配；自定义角色创建时复制模板角色矩阵。三层权限分工：功能矩阵管页面开关、数据范围规则代码内置、流程权限在状态机/流程定义页。
- 业务域=横向服务线：负责人（BM）+ 服务团队成员；用户组=纵向专业线资源池：负责人（TM）+ 组授予角色。

### 2.2 自定义角色与用户组（2026-07-10 增补，M3.5 扩展）

- **内置角色**（共 16 个）：承载系统功能权限，代码只读不可删改；名称和描述可维护。auditor = 全模块只读 + 审计日志查看，不能修改任何单据（仅持有 auditor 的用户受全局只读限制；auditor+其他角色不受限）。
- **自定义角色**：admin 可创建，必须**继承一个内置角色**（获得其系统权限，不允许继承 admin），同时可被状态机流转规则和流程步骤精确引用（如自定义"DBA"继承 it_ops，变更审批仅限 DBA）。被用户持有或被流程/状态机引用的角色不可删除。
- **用户组**：团队/派单单位（如"数据库组"），**可授予角色——人进组自动继承组的角色**（对齐 ServiceNow"角色挂组"最佳实践；admin 不允许经组授予）；同时用作流程步骤默认指派对象和状态机流转的授权对象（引用键格式 `group:组码`）。被引用的组不可删除。
- **有效角色 = 直接角色 ∪ 组授予角色 ∪ 自定义角色继承**；用户永远可持有多个角色，任何规则都不得将用户绑死在单一角色上。

### 2.2a 身份与组织（M3.5，详见 docs/06）

- **部门**：公司组织架构（一人一部门，树形，类型 it/business/audit），纯数据不带权限；飞书已作为人员与部门主数据源，本地记录仍支持过渡性手工维护。
- **账号开通（JIT）**：local 与飞书认证已实现（ad/sms/wechat 预留）；飞书新身份认证成功后创建待审批开通请求，由管理员关联人员、设置用户名/角色/语言后开通；**开通规则**（按部门类型或具体部门→默认角色）仅首次生效，之后角色自由增减。
- **业务域/服务线**：编码/名称/负责人（BP）/备份负责人——负责人是字段不是角色。

### 2.3 权限要点

- 登录后按角色渲染菜单；接口层同步校验（前端隐藏 ≠ 权限）。
- 流程引擎的步骤默认指派可引用：内置角色、自定义角色（含继承匹配）、用户组，如：需求评审→it_bm、方案评估→it_pdm_leader、项目节点→it_pm、变更申请/实施→it_ops、变更审批→it_op_leader、变更复盘→is_mgr、需求开发任务→it_dev_leader。
- 状态机每条流转规则可限定允许的角色/用户组（留空=不限）。
- 人效评审明细按业务/专业线负责人范围和 CIO 终审权限开放；被评价者只能查看已发布的本人最终结果，成员仍可查看自己的积分流水与排行榜。
- 岗位编制默认由 admin/cio 维护，岗位定义与招聘需求均具备查看、编辑、删除权限；页面清单支持行内编辑保存。
- 审计：所有写操作记录审计日志（操作人/时间/实体/动作/变更摘要）；admin 与 is_mgr 可查。

### 2.4 账号治理与个人中心（M36–M37）

- **账号删除**：具备用户删除权限的管理员可软删除账号并解绑关联人员；人员主数据、部门归属和历史单据不删除。内置 `admin` 与当前登录账号不可删除；删除后释放原用户名，允许后续重新开通。
- **账号与人员解绑**：管理员在用户编辑页清空“关联人员”并保存后，账号的 `person_id` 必须置空；后续刷新仍保持未关联。PATCH 未提交该字段时保持原关联不变，避免把局部编辑误判为解绑。
- **飞书认证**：支持浏览器扫码 OAuth 与飞书工作台 JSAPI 免登；两者复用身份匹配、首次开通审批和正式登录逻辑。
- **飞书服务台交接（M45）**：员工在飞书服务台询前单完成转人工后，ITOM 在原服务台会话提供“创建 IT 服务请求”和“登记 IT 需求”两个稳定入口；链接只包含待分流记录 ID 和动作，不携带用户身份或一次性令牌。员工点击后如未登录先完成 ITOM 登录，后端再核对账号绑定的 Feishu `open_id`、待分流申请人和实时工单 guest 三者一致，随后签发默认 10 分钟有效的一次性令牌并直接打开预填页面。服务类别只映射 ITSM 服务目录/服务项，不映射需求业务域；询前单的标题、紧急程度、问题描述、其他补充信息、申请人和客服作为来源上下文带入 ITOM。建单成功即消费令牌并禁止再次创建；同一稳定入口再次打开时进入已关联单据。IT 需求的需求分类和业务域仍由业务人员在 ITOM 登记页选择。
- **飞书服务台可靠同步（M46）**：转人工后的工单先建立“待分流记录”；飞书工单事件按 `event_id` 幂等入队，由后台异步重试并重读当前工单详情。分流入口优先作为富文本写入原服务台会话，不支持时降级为带完整 URL 的文本；原会话连续投递失败后，才使用独立应用机器人动态卡片兜底。员工建单后，交接令牌将最终单据与飞书工单关联。ITOM 只把已登记、已分派、处理中、已完成、已关闭等用户可见进展回写原会话，内部备注、审批理由和未发布信息不外发；管理员可查看入口投递渠道、待分流与失败事件队列。
- **个人中心**：用户可查看账号来源、有效角色、创建/最近登录时间及关联人员主数据；HR 字段只读，由飞书组织同步维护。
- **安全设置**：本地账号及已设置过密码的账号修改密码时必须校验当前密码；飞书开通账号的随机初始口令不可知，首次自设本地密码免验当前密码。新密码至少 8 位且同时包含字母和数字。
- **飞书绑定管理**：登录用户可绑定或换绑飞书身份；同一飞书身份不可绑定多个账号。解绑前必须先设置本地密码，确保仍有可用登录方式，且解绑不影响人员主数据关联。
- **初始密码治理（M44）**：管理员批准飞书登录申请时生成 12 位高强度密码，仅保存可恢复加密密文，不自动发送。用户详情默认隐藏，管理员点击眼睛后才解密查看，点击“邮件发送”才投递到关联人员邮箱；两项操作写审计。用户改密或管理员重置后清除初始密码密文。
- **系统集成（M44）**：系统管理下统一提供飞书、邮件服务器、AD/LDAP 三个标签页。SMTP 与 LDAP 绑定密码加密保存；LDAP 支持连接测试及同名 ITOM 活动账号的目录密码认证，角色权限继续由 ITOM 管理。
- **个人设置**：支持头像、个人说明、语言、通知分类偏好、浅色/深色/跟随系统主题、默认/紧凑内容密度；偏好按账号持久化并在登录后应用。方案 C 工作台与方案 F 门户均将当前主题状态直接绑定到应用壳层；深色主题统一采用画布、页头、模块三级深灰表面而非白色外壳或纯黑画布，导航、标签页、分段选择器及其他交互状态使用配套深色配色，并满足清晰可读的文字对比度。
- **帮助中心式操作手册**：登录后的应用顶栏在语言切换按钮左侧提供“操作手册”入口，所有已登录角色均可通过搜索、产品分类、热门/最近文档和文章内目录查看当前版本的系统介绍、模块运作逻辑、操作步骤、角色边界和常见问题；内容与 `docs/用户操作手册.md` 同步维护。
- **个人操作记录**：用户仅能查看自己的审计记录，不暴露其他账号的审计数据。

---

## 3. 全局约定

### 3.1 单据编号与主键

- 所有实体主键为 26 位 GLID（系统生成）。
- 业务编号自动生成，规则：`前缀-年月-序号`，如 `TK-202607-0012`（工单）、`PJ-202607-003`（项目）、`RQ-202607-021`（需求）、`PB/KB/ID` 等。用户永远不手填编号。

### 3.2 状态机

每类单据的状态流转由状态机配置驱动（admin 可调整），非法跳转被后端拒绝。各实体默认状态机见对应章节。

### 3.3 通知

- 统一事件出口：工单创建/指派/SLA 临期/升级、变更待审批、里程碑逾期、需求阶段流转、建言被采纳等事件写入通知发件箱。
- 初期仅实现**站内通知**（顶栏铃铛 + Dashboard 告警区）；后续增加飞书通道适配器即可接入，业务功能零改动。
- M34 起，未关联人员档案的管理员账号也可通过账号 ID 接收开通申请等管理通知；铃铛定时轮询新通知并按 `work`、`workflow`、`system` 三类个人偏好过滤。
- 通知弹窗提供“一键已读”和“清除已读”：前者只更新当前账号可见通知的 `read_at` 回执，后者以软删除方式清理当前账号已读通知；两者都不影响源业务单据。

### 3.4 页面清单

登录、总览、个人中心（账号/安全/个人设置/操作记录）、工单列表/详情、服务目录、CMDB、问题、供应商、合同、知识库、项目管理（项目列表/项目组合两个标签页）、项目详情、需求列表/详情、流程定义、流程监控、团队总览、人效评分、岗位编制、学习成长（页面内含“培训提升”“学习任务”两个标签页）、建言献策、团队文化、系统管理。

---

## 4. 域一：总览 Dashboard

**目标**：单页看清团队全貌，全部指标实时计算，无任何录入。

### 4.1 页面结构（四板块 + 告警区）

| 板块 | 指标 | 点击行为 |
| --- | --- | --- |
| 服务请求 | 未关闭数、P1–P4 处理级别分布、本月解决数、本月 SLA 达成率 | 跳转服务请求列表（带筛选） |
| 变更 | 未关闭数、P1–P4 处理级别分布、待审批、实施中、成功率 | 跳转变更列表（带筛选） |
| 事件 | 处理中数、P1–P4 处理级别分布、SLA 临期、本月解决数 | 跳转事件列表（带筛选） |
| 问题 | 进行中数、P1–P4 处理级别分布、已知错误、关闭率 | 跳转问题列表（带筛选） |
| 项目 | 在建项目数、健康度分布（绿/黄/红）、逾期里程碑数、预算执行率 | 跳转项目列表 |
| 需求 | 四阶段需求数量（登记/分析/实现/关闭）、本月平均交付周期 | 跳转需求列表 |
| 团队 | 人员负载 Top5、成长积分排行 Top5、本月培训场次、招聘进度 | 跳转团队管理 |
| 告警区 | SLA 临期/超时工单、合同 90 天内到期、里程碑逾期、红色健康度项目 | 跳转对应单据 |

### 4.2 验收标准

- [ ] 页面单次加载 ≤ 2 秒（一次聚合查询，不允许每个指标一次请求）
- [ ] 任一指标数字与对应列表页筛选后的记录数一致
- [ ] 无任何输入控件

---

## 5. 域二：ITSM 服务管理

### 5.1 工单（核心，单表多类型）

工单类型：**事件 incident / 服务请求 service_request / 变更 change**。问题是独立实体（5.2）。

#### 创建表单

服务请求面向业务用户采用与飞书服务台询前单一致的字段顺序：用户（只读，当前登录人或飞书交接申请人）、标题、紧急程度、服务类别、具体服务项、问题描述、其他补充信息、接单客服（只读，飞书交接时带入，未分派时显示待后台分派）。服务类别对应 ITSM 服务目录，不映射需求业务域；目录下仅有一个上架服务项时自动选中，多个时要求用户选择具体服务项。IT 内部角色和系统管理员可展开“内部处理信息”补充受理人和内部备注。

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| 标题 | ✔ | |
| 类型 | ✔ | 三选一，默认"服务请求" |
| 紧急程度 | ✔ | P1 紧急 / P2 高 / P3 一般 / P4 低，默认 P3 |
| 服务类别 | ✔ | 来自 ITSM 服务目录；不映射需求业务域 |
| 具体服务项 | ✔ | 服务类别唯一匹配时自动带出，否则下拉选择；带出服务线与 SLA 目标 |
| 问题描述 | ✔ | 与飞书询前单字段一致 |
| 其他补充信息 |  | 与飞书询前单字段一致 |
| 受理人 | 可选 | 仅 IT 内部角色/管理员在“内部处理信息”中填写；不填则按流程默认指派 |
| 关联 CI | 可选 | 折叠在"更多" |
| 备注 | 可选 | 折叠在"更多" |

选择"变更"类型时追加展开：变更类型（标准/普通/紧急）、风险等级、变更原因、回退方案、变更窗口（计划开始/结束）、实施方案（可选）。

#### 自动化（用户不填）

编号、状态、提交人及部门（当前登录人带出，可代提）、四个时间戳（提交/首次响应/解决/关闭）、SLA 目标（按服务项策略匹配）、实际响应/解决时长、SLA 是否达成、是否一次解决（按重开推断）。

#### 生命周期与状态机

```text
事件/服务请求：新建 → 处理中 → 已解决 → 已关闭
                ↘ 挂起 ⇄ 处理中          ↘ 重开 → 处理中
变更：新建 → 待审批 → 已批准 → 实施中 → 已解决 → 已关闭
              ↘ 已拒绝(终态)                ↘ 已回退 → 已关闭
```

**当前已发布的变更流程定义（运行时基线）**：以流程中心中激活的 `change_flow` 为准，当前为四步，不把代码种子或旧版文档当成运行时事实：

`变更申请（it_ops，处理） → 变更审批（it_op_leader，审批；知会 it_bm） → 实施与验证（it_ops，处理） → 变更复盘(PIR)（is_mgr，处理；知会 cio）`

其中，变更申请和实施任务由 IT 运维处理；审批节点的默认任务处理人是 IT 运维负责人，不等同于状态机允许的全部审批角色。状态机当前允许 `cio`、`it_tm`、`it_op_leader` 对 `pending_approval` 执行批准/拒绝。运行时流程定义、状态机配置和已发布版本优先于 `seed_itsm.py` 的新环境初始化种子；如需修改节点，必须在流程中心另存新版本，历史实例继续使用原版本快照。

- **解决时**填写：解决方案（必填）、根因（可选）。
- **关闭时**填写：关闭代码（下拉）。关闭后向提交人发起满意度评价（1-5 星，可选）；未评价时入口显示在工单详情标题栏右侧，提交后该入口消失，结果在基本信息中只读展示。
- 变更审批：当前已发布的 `change_flow` 将“变更审批”任务默认指派给 `it_op_leader`；状态机 `pending_approval → approved/rejected` 的 `allowed_roles` 为 `[cio, it_tm, it_op_leader]`（可在状态机配置中调整）。审批附意见并记录审批人与时间；流程任务守卫与状态机授权需同时通过。
- 升级：P1/P2 工单超 SLA 目标 80% 未解决时产生升级通知。

#### 验收标准

- [ ] 服务请求从打开表单到提交 ≤ 30 秒（5 个必填项）
- [ ] 变更工单不经审批无法进入实施中
- [ ] SLA 达成自动判定正确（含挂起时间扣除）
- [ ] 工单解决自动产生积分事件

### 5.2 问题管理

来源：手工创建、工单升级转入（一键，自动带描述与关联）、需求关闭时遗留问题转入。

- 创建 3 必填：标题/描述/优先级；可选关联服务项。
- 阶段填写：根因、临时规避方案。
- 状态机：`新建 → 分析中 → 已知错误 → 已解决 → 已关闭`。
- 列表展示关联工单数（同根因工单挂接），支持从问题反查工单。

### 5.3 服务目录

两层结构：**目录**（分类，Gold/Silver/Bronze 分级）→ **服务项**。

- 服务项字段（10）：名称✔、所属目录✔、服务类型、负责人、描述、SLA 响应时限（小时）、SLA 解决时限（小时）、服务对象、状态（自动）、编号（自动）。
- 服务对象为结构化范围：可选“全体员工”或“自定义范围”；自定义范围从组织架构中选择部门和在岗员工，服务项保存时校验引用有效，并保留可读摘要兼容列表、搜索和历史记录。业务用户服务门户和服务请求创建接口均按范围进行后端校验；管理员及 IT 内部角色不受该业务用户范围过滤影响。
- 服务项年度工单量为实时统计展示，不录入。
- 管理员/CIO可在目录与服务项清单中直接上架或下架（上下箭头按钮）；目录下架不会删除历史服务项，服务项下架后不再出现在业务用户服务门户。
- 服务项清单支持关键字搜索、按上架状态筛选（全部/上架中/已下架）、点击任意业务字段表头排序，并提供目录浏览前进/返回操作；左侧目录同时展示每个目录的上架中/已下架服务项数量；系统所有表格统一支持可点击表头排序。
- 删除目录时，管理员可在二次确认后级联软删除该目录下的服务项；历史工单、项目和配置项保留原服务项引用，不因目录清理而破坏审计链路。
- **统一表格交互标准**：凡展示业务记录的表格，必须提供关键字搜索、按核心字段及其值筛选、点击表头排序；已接入批量写入的可编辑主数据（服务目录/服务项、CMDB、供应商、合同、需求、岗位定义、招聘需求、WBS）提供 Excel 模板下载、填写后批量导入及逐行错误反馈。其余系统配置与事务单据仍使用详情表单逐条维护；只读审计、流程运行态和计算结果表提供当前结果导出，但不允许反向导入覆盖系统计算结果。
- 维护权限由权限矩阵控制（默认 cio/admin）；全员可浏览（作为报单入口参考）。

### 5.4 CMDB 配置管理

单表 CI，9 个类别：应用 / 服务器 / 云资源 / 网络 / 安全 / 协作 / 终端 / 基础设施 / 咨询服务。

- 创建 4 必填：名称、类别、状态（默认运行中）、负责人。
- 可选：环境、业务负责人、供应商（关联供应商表）、描述、上线日期、备注。
- **类别专属属性**：每类预定义少量扩展属性（如应用：技术栈/部署方式；服务器：IP/配置），存 JSONB，折叠展示，全部可选。
- CI 关系：支持"运行于/依赖/连接"三种关系，用于影响分析（查看某 CI 时展示上下游）。
- 工单可关联 CI；CI 详情页反查关联工单历史。

### 5.5 SLA 管理

- **SLA 策略**（admin 配置）：按优先级 P1-P4 定义默认响应/解决时限；服务项可覆盖。
- **SLA 看板**（只读）：本月/本季各优先级达成率、超时工单清单、临期预警。全部实时计算。

### 5.6 供应商与合同

- 供应商（9 字段保留）：名称✔、联系人、电话、邮箱、服务范围、评级、状态、备注、编号（自动）。
- 合同：名称✔、供应商✔、金额、起止日期✔、负责人、状态（自动按日期推）、备注、附件。
- **到期预警**：距到期 90 天进 Dashboard 告警区并发通知。

### 5.7 知识库

- 发布仅 2 必填：**标题、正文**（Markdown）；可选：标签、关联工单。
- 自动：编号、作者、浏览数、"有用"数。
- 全文检索；工单详情页可一键"沉淀为知识"（带出工单上下文草稿）。
- 来源之三：需求关闭时经验沉淀一键转入。
- 发表知识、知识被点"有用"均产生积分事件。

---

## 6. 域三：项目管理

### 6.1 项目创建

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| 名称 | ✔ | |
| 项目经理 | ✔ | 默认当前人 |
| 计划开始/结束 | ✔ | |
| 所属组合 | 可选 | 下拉选择已创建的项目组合（在"项目组合"标签页维护，见 6.5） |
| 描述 / 预算(万) / 关联服务项 | 可选 | 折叠 |

**或者：上传项目章程文档（.docx），系统解析自动生成项目 + WBS + 里程碑 + 风险草稿，用户仅确认修正**——推荐路径，解析失败时回退手工表单并提示原因。

自动：编号、状态、实际起止（状态流转打点：转进行中/已完成时仅在为空时写入，**支持手动编辑覆盖**，M12）、进度（WBS 汇总）、健康度（规则：进度偏差 >15% 或里程碑逾期 → 黄；>30% 或红色风险 → 红）、实际成本（成本明细汇总）。

状态机：`规划中 → 进行中 → 已完成 → 已关闭`，可从进行中转"已暂停/已取消"。
恢复流转（M12）：已关闭→进行中（重新启动，可选择流程回退到指定节点、实际结束清空）、已完成→进行中（重新打开）、已取消→规划中（重新规划）。项目关键节点流程为任务驱动：完成当前步骤（**必填阶段结束说明 ≥5 字**）自动推进下一节点；“立项启动”和“执行监控”始终绑定项目主数据中指定的项目经理（不从 `it_pm` 角色持有人中重新选人），流程回退后也保持该绑定；“收尾复盘”默认按 `it_pmo` 角色指派。

### 6.2 项目详情 5 个标签页

| 标签页 | 内容 |
| --- | --- |
| 概述 | 基本信息 + 健康度 + 最新动态（一句话，随时更新）+ 关联需求 + **章程信息分段**（M13：项目背景/项目目标/范围-做什么与不做什么/预算与资源说明/项目组织-主要成员与关键干系人/其他说明，均结构化字段可编辑，与章程模板章节一一对应，导入自动填充） |
| 进度 | **甘特图**（按 WBS 起止渲染，含依赖线与里程碑标记）+ WBS 树 + 里程碑列表 |
| 成本 | 预算、成本明细录入（日期/金额/说明，极简 3 项）、自动汇总执行率、SPI/CPI 简化展示（按工期权重） |
| 风险 | 风险登记（标题/概率/影响/应对措施 4 项）、概率×影响矩阵图 |
| 文档 | 附件上传列表 + 章程导入入口 |

### 6.3 WBS 任务

- 创建 4 必填：任务名、负责人、开始/结束日期；可选：描述、交付物、前置任务。
- WBS 层级编码自动生成（按树位置）；完成度提供 0%/50%/100% 预设下拉，同时支持录入 0-100% 的自定义整数，状态按完成度与计划结束日自动计算（0% 未开始、100% 已完成、其余为进行中/已延期）。显式将任一父项设为 100% 时，所有后代任务自动设为 100%；修改子项后，父项按直接子项完成度的算术平均值逐级回算，项目整体进度仅对末级任务按工期加权，避免父子重复计权。
- WBS 宽表提供 Excel 式冻结窗格：纵向滚动时表头固定，横向滚动时左侧前三列固定；表格提供一条底部悬浮横向滚动条；用户可拖拽表头分隔线调整列宽、拖拽行底边调整行高，布局按项目保存在当前浏览器。
- 所有超出内容区宽度的表格统一提供一条底部悬浮横向滚动条，并在页面纵向滚动时悬浮表头；滚动到表格底部或表格主体离开视口时，当前活动表的滚动条仍保持可操作，同一视口只显示一条活动表滚动条；WBS 额外提供前三列冻结。
- 任务按期完成产生积分事件。

### 6.4 里程碑

名称✔、目标日期✔、说明；实际达成日期在达成时点一下；逾期自动告警。

### 6.5 项目组合（项目管理页内标签页）

项目管理主页面分两个标签页：**项目列表**（默认）和**项目组合**。

- 项目组合标签页：维护组合定义——名称✔、负责人、年度、描述；列表展示每个组合下的项目数与健康度分布。
- 创建项目时"所属组合"下拉选择已创建的组合名称（组合须先在此标签页创建）。
- Dashboard 与项目列表支持按组合筛选。

### 6.6 验收标准

- [ ] 手工建项目 ≤ 1 分钟；章程导入路径除确认外零手填
- [ ] 甘特图正确渲染 3 层 WBS 与依赖线
- [ ] 健康度/进度/成本执行率全自动，与明细数据一致
- [ ] 里程碑逾期次日出现在 Dashboard 告警区

---

## 7. 域四：需求管理（评估漏斗 · M10 升级）

### 7.1 五阶段生命周期

M10 起：在「登记」与「分析」间插入「评估」门——六维加权评分 + 四象限决策。**M16 定稿完整分流闭环**：登记即进入评审（评审任务按业务域自动指派服务线负责人）；评分落「重新评估」象限的仅可 搁置（补充后重评）或 驳回（关闭，必填理由）；其余象限立项后转专业线（产品 leader 主责/开发 leader 知会，可配置）做**方案评估与路径判定**——已有系统二次开发且人天<阈值(默认 20，可配)→需求开发实现（任务清单排期，优先级=六维加权总分）；新购系统 或 人天≥阈值→**转项目管理**（指派 PM→PM 备章程建项目并关联需求）。**闭环（M16.5）**：项目验收关闭→提醒 PM 回需求完成「实现交付」步骤→「验收与闭环」任务自动指派业务域负责人（组织业务部门验收）→流程完成时需求自动关闭（验收标准未全勾则提醒手动关闭）。决议保存即自动流转（评审动作一步到位）。

```text
登记 → 评估 → 分析 → 实现 → 关闭（可从任意在办阶段转"搁置/取消"）
```

| 阶段 | 谁 | 填什么 | 系统做什么 |
| --- | --- | --- | --- |
| ① 登记 | requester/it_bp/任意团队成员 | 标题✔、类型✔（业务/功能/数据/集成/合规）、所属业务✔、描述✔；来源/渠道部门/期望完成时间/期望效果/运营价值 可选 | 编号、提出人、登记时间自动；通知 it_pdm |
| ② 评估 | it_pdm + 评审（CIO/it_bm 知会） | 六维评分 D1-D6（1-5）、决议（立项/搁置/驳回）、备注 | **自动算加权总分与四象限**（不手填）；进入评估时间打点；评估门校验 |
| ③ 分析 | it_pdm | 负责人、排期目标日期、解决方案、验收标准（清单式）、PRD/开发人天；MoSCoW 降级为可选辅助标签 | 进入分析时间打点 |
| ④ 实现 | 负责人 | 任务分解（任务名/负责人/计划日期/计划·实际工天）、可选挂接项目 | 进度=已完成任务/总任务；任务完成产生积分 |
| ⑤ 关闭 | 负责人 | 逐条勾验收标准；**一键转出**：遗留问题→ITSM 问题、经验→知识库（自动带需求上下文） | 完成日期打点；交付周期计算；关闭产生积分 |

### 7.2 六维加权评分与四象限

依据《需求评分与优先级细化标准》（麦肯锡战略价值-执行可行性双轴）。权重/阈值/分档在**需求管理→「评分规则」标签页**配置（admin 可编辑，其他角色只读参考分档说明），年度可调（默认见下）。

| 维度 | 默认权重 | 说明 |
| --- | --- | --- |
| D1 战略对齐 / D2 业务价值 / D3 技术可行性 | 0.2 / 0.2 / 0.2 | 战略价值 + 技术 |
| D4 组织就绪 / D5 风险等级(反向) | 0.1 / 0.1 | 风险按 (6−D5) 反向计入 |
| D6 价值速度 | 0.2 | Time-to-Value |

`加权总分 = 0.2·D1 + 0.2·D2 + 0.2·D3 + 0.1·D4 + 0.1·(6−D5) + 0.2·D6`（服务端计算）。
四象限（横轴战略价值 (D1+D2)/2，纵轴总分，默认阈值 total 3.5 / strategic 4 / viable 3）：⭐战略下注、⚡速赢项目、📋低优先级、🔄重新评估。
评审治理：M10 起步为**单人共识评分**（`requirement_score.is_consensus` 回填需求六维分），多方独立评分+共识为后续增强（表已预留 reviewer_role/role_weight）。

### 7.3 需求列表与导入

- 按阶段分栏（看板，含「评估中」栏）+ 表格视图切换；筛选：业务线/优先级/**决议**/负责人/提出人；卡片展示加权总分与象限。
- **模板导入**：下载 Excel 模板（登记字段 + 可选六维分）→ 填写 → 上传，部分成功报错到行；已评满六维的行导入即落「评估中」并带分。

### 7.4 验收标准

- [ ] 登记 4 必填 ≤ 30 秒完成；评分字段只在评估阶段出现，不进登记表单
- [ ] 加权总分/象限全部服务端派生，用户不可手填
- [ ] 评估门：六维未评满或决议非「立项」时不允许进入分析
- [ ] 验收标准未全部勾选时不允许关闭（有遗留时必须转出问题或说明）
- [ ] 转出的问题/知识自动携带需求编号反向链接
- [ ] 全域中英双语；权重/阈值/分档 admin 可配

---

## 8. 域五：流程引擎（简化版）

**定位**：为工单/变更/需求/项目提供可配置的步骤化流程与自治级别标注，不做重装置。

- 三层模型：**流程定义**（含步骤：名称/顺序/节点类型〔处理/审批〕/默认角色/知会角色/L1-L4 自治级别/SLA 时限）→ **流程实例**（单据触发）→ **流程任务**（步骤实例，指派到人）。审批节点支持右上角同意（理由可选）或驳回（理由必填），处理节点使用“完成此步骤”推进。
- 自治级别：L1 全自动 / L2 系统建议人确认 / L3 人做系统辅助 / L4 纯人工。用于流程监控展示与后续人效分析，不控制执行。
- 初始内置 6-8 个流程：事件处理、服务请求交付、变更审批实施、问题分析、需求交付、项目关键节点。
- 派单规则：按步骤默认岗位角色指派（如需求评审→it_bm、方案评估→it_pdm_leader、项目节点→it_pm、变更申请/实施→it_ops、变更审批→it_op_leader、变更复盘→is_mgr、开发任务→it_dev_leader），可在实例中改派，无外部依赖。

**当前已发布运行时的六条内置流程快照（2026-07-24 本地运行时核对）**：下表是流程中心中激活版本的实际节点，不是泛化的 ITIL 示例，也不是旧版种子说明。流程中心后续发布新版本后，以新版本为准；历史实例保留原快照。

| 流程 | 实际步骤（默认处理人；括号内为节点语义/知会） |
| --- | --- |
| `incident_flow` | 受理定级（it_op_leader，审批）→ 诊断与处理（it_ops）→ 解决与用户确认（it_ops，审批）→ 关闭复盘（it_op_leader） |
| `sr_flow` | 受理确认（it_ops）→ 实施交付（it_ops）→ 用户确认关闭（requester，审批） |
| `change_flow` | 变更申请（it_ops）→ 变更审批（it_op_leader，审批；知会 it_bm）→ 实施与验证（it_ops）→ 变更复盘(PIR)（is_mgr；知会 cio） |
| `problem_flow` | 问题确认（专业线负责人动态指派，审批）→ 根因分析（负责人指定处理人）→ 解决与验证（延续处理人）→ 解决确认与关闭（专业线负责人，审批） |
| `requirement_flow` | 需求评审（it_bm，审批）→ 方案评估与路径判定（it_pdm_leader，审批）→ 实现交付（it_dev_leader/项目经理）→ 验收与闭环（it_bm，审批） |
| `project_flow` | 立项启动（it_pm，审批；知会 cio、it_bm）→ 执行监控（it_pm）→ 收尾复盘（it_pmo，审批；知会 cio、it_tm） |

服务请求流程不新增人工分流节点：飞书服务台只负责询前沟通和类型选择，正式 ITOM 单据消费交接令牌后进入上述三节点。流程任务自动指派/改派发布 `ticket.assigned`，最后一个 requester 节点完成发布 `ticket.user_confirmed`，随后由既有状态机完成 `resolved` → `closed`；飞书评价事件写回 `Ticket.satisfaction`。这些事件是五个同步节奏点的内部事实来源，出站时只保留用户可见进展。

- **流程节点标准（M3.8，后续所有流程功能强制）**：每个节点可配置**处理人**（审批/执行，产生任务、阻塞流程）与**知会人**（仅站内通知，不产生任务、不阻塞，可多个角色/用户组）；流程定义页展示**流程示意图**（处理人蓝标/知会人灰标），编辑器实时预览。详见 docs/06 §十。
- 页面：流程定义管理（admin，含启停/编辑步骤）、流程监控（实例列表、卡点步骤、超时任务）。
- 单据详情页展示流程条（当前步骤高亮）。

**关于 n8n**：流程引擎内置于后端代码，不引入 n8n。理由：原系统 n8n 承担的四项职责中，文档自动生成已裁撤、飞书通知初期不接、按技能/负载派单简化为按角色指派、事件编排由通知发件箱覆盖；而 n8n 作为独立服务的部署维护与跨系统排障成本，正是旧系统复杂度的来源之一。**预留**：通知发件箱的事件流就是外部编排工具的挂接点，未来如需跨系统自动化，n8n 可订阅发件箱事件接入，业务代码零改动。

**验收**：新建工单自动创建对应流程实例；流程监控可见每个实例进行到哪一步；改派生效。

---

## 9. 域六：团队管理

**统一统计边界（M40）**：本模块只统计和展示 IT 团队成员，不使用全公司人员口径。IT 团队以组织架构中 `dept_type=it` 为主；未标部门的存量账号按 `cio`、`it_*`、`is_mgr` 等 IT 角色兼容识别。业务部门人员及仅持 requester/auditor 的人员不进入团队总览、工作负载、积分排行、人效、活动发放和岗位在岗数；团队页面的人员选择器同样只返回 IT 成员。

### 9.1 人效管理（分级评审）

- **矩阵角色评分**：CIO 以员工当前业务服务线角色、专业线角色和团队贡献配置评分；业务角色权重 + 专业角色权重固定为 80%，团队贡献固定为 20%。
- 每个角色独立 100 分制；业务服务线由业务线负责人/BP 初评，产品/项目/开发/运维专业线由对应负责人初评，平台服务角色由 CIO 直接评分；CIO 不纳入本套常规评分。
- 负责人只能修改自己管理范围内对应条线的角色分，不能修改另一条线、团队贡献原始积分或外部原始数据；负责人不得评价自己。CIO 查看全部系统参考分和负责人初评，并进行终审、微调以及负责人本人的评分。
- 评分状态为 `draft → auto_scored → external_input → manager_review → cio_review → published → locked`。系统初评、负责人初评、CIO 终审和最终生效分分开保存，发布后重新调整必须建立新版本。
- 业务满意度等系统外事实通过独立的外部原数据页面录入、核验、锁定和修订，记录评价人、原始分、量表、折算分、说明和证据；外部业务满意度与 ITSM 工单满意度分开保存。
- 发布前评审明细仅对有范围权限的负责人和 CIO 可见；被评价者只能访问发布后的最终结果，不显示系统参考分、负责人/CIO 微调和外部评价明细。
- 团队贡献由专项活动、学习成长、培训知识分享、建言改进、知识复盘、跨团队支援六个维度构成，满分 100 后按固定 20% 计入。
- 工单、需求、项目和流程事实若已用于角色结果指标，标记为 `role_result`，不得再次计入团队贡献；只有职责之外的活动、学习、分享和跨团队支援等 `team_contribution` 进入固定 20%。
- 评分周期沿用 `YYYY-Q1/Q2/Q3/All`；角色、权重、评分主体和规则均生成周期快照，发布后可审计、可复现。
- 详细角色维度、权重、评分主体、学习成长任务和数据模型见 [IT 团队角色绩效评分 PRD](08-IT团队角色绩效评分PRD.md)。
- “人效评分 → 计分规则”以矩阵角色档案为当前生效设计：展示角色维度权重、系统/外部/人工取数方式，以及当前考核期员工的业务角色/专业角色 80% + 团队贡献 20% 权重矩阵；系统管理员与 CIO 可进入并验证规则。旧版 `perf_scheme` 接口保留作历史兼容，不再作为页面主设计。
- “人效评分 → 总览”读取矩阵角色周期结果，按员工展示角色职责结果、团队贡献 20%、加减分和当前总分；页面不再展示旧版“默认方案”或旧版岗位维度列。当前模型没有全局默认方案：新增规则使用“计分规则 → 新增角色规则”，再在周期员工详情中分配角色和权重。
- “人效评分 → 分级评审”总览按员工聚合为一行；点击员工的“评分明细”进入独立详情页，查看并按权限调整该员工全部角色权重、评价维度、系统参考分、负责人初评、CIO 终审/微调、生效分及修正说明。

### 9.2 成长积分

**核心机制：积分零录入，事件自动计分。**

| 来源 | 事件（分值全部可配，支持负分） |
| --- | --- |
| 服务贡献 | 工单解决、SLA 双达成、满意度 ≥4 星、问题根因定位 |
| 项目贡献 | WBS 任务按期完成、里程碑交付 |
| 需求贡献 | 需求任务完成、需求关闭 |
| 培训贡献 | 主讲/组织培训、参与培训 |
| 知识贡献 | 发表文章、文章被点"有用" |
| 建言献策 | 提出建言、被点赞、被采纳 |

- 积分流水：人/事件/分值/来源单据/时间，可追溯到触发单据；管理员手工加减分同样走流水（需填理由）。
- 页面：排行榜（月/季/年/累计）、个人积分明细、活动积分→积分规则（团队贡献活动规则，仅 admin/CIO 可修改；所有修改写入审计日志）。岗位职责结果的取数、角色维度、RACI/流程节点映射和权重在团队管理→人效评分→计分规则维护，两者不可混算。
- 积分作为 9.1 人效评价输入源之一。

### 9.3 岗位编制

- 人员主数据：姓名✔、部门、岗位（关联岗位定义）、团队分组、状态（在岗/离职）、入职日期、技能标签。
- 岗位定义：岗位名✔、岗位编码、岗位族/序列、服务业务域、主责角色、职级/能力框架、办公地点、关键技能、是否允许外包、启停状态、分工职责描述和正式编制数；页面实时呈现“正式编制 vs 正式在岗 vs 缺口”。
- 正式编制只统计 IT 团队正式在岗人员，外包/实习人员不计入正式编制；岗位与人员通过 `position_id` 关联，飞书组织同步不覆盖本地岗位关系。
- 招聘需求：岗位✔、级别（高级/中级/初级）、人数✔、任职资格✔、状态（待招聘/面试中/已到岗/取消）、进度备注；一个岗位可有多条分级、分批招聘需求。
- 岗位定义和招聘需求均支持“导出 Excel / 下载模板 / 批量导入”：导出文件带字段说明行，可修改后回导；按岗位编码/名称匹配，支持新建与更新，失败行返回行号和原因并写入审计日志。可恢复的非法列宽会自动清洗；其他损坏文件、超长字段、编码/名称冲突和已删除岗位编码等异常均转换为可读的逐行导入结果，不以 500 中断请求。
- 岗位定义与招聘需求的编辑、删除权限默认授予 `admin` 和 `cio`；列表页提供行内编辑、保存、取消和删除，修改不必进入详情弹窗。删除采用软删除，仍被人员或招聘需求引用的岗位会先阻止删除。

### 9.4 培训提升

- 活动登记 5 项：类型✔（内部交叉培训/外部技术交流/新技术研究）、主题✔、日期✔、主讲/组织人、参与人（多选）+ 可选产出链接。
- 登记完成自动为主讲人与参与人计积分。

### 9.5 团队文化

单页富文本管理：愿景、目标、工作行为准则（manager/admin 编辑，全员可见）。

### 9.6 建言献策

- 提交仅 2 项：标题✔、内容✔。
- 全员可浏览、点赞（每人每条一次）；manager 可标记"已采纳/已实现/已婉拒"。
- 提出/被点赞/被采纳自动计积分；被采纳的建言可一键转需求登记。

---

## 10. 支撑：系统管理（admin）

菜单结构（8 入口）：

1. **组织管理**：「组织架构」标签页（公司→部门→人员三级树 + 右侧主数据面板；**飞书为 Source of Truth**；管理员在同步完成后配置“数字化团队范围”）。范围由所选部门成员（可包含下级部门）与单独指定人员组成并集，因此可从 Test 等混合供应商组织中只纳入需要参与 IT 业务和绩效考核的外包人员，而不纳入同部门其他人员。该口径统一驱动团队统计、绩效对象及所有业务人员选择器。「业务服务域」标签页维护负责人 + 备份负责人 + 服务团队 + 服务部门。项目经理、项目任务负责人、需求负责人/评审人/开发负责人、工单/问题/服务项/配置项/合同负责人、用户组负责人及成员等下拉选择均只展示该数字化团队范围；前端通过 `scope=it` 获取，后端在已配置范围后再次校验，不能依赖前端过滤。业务域支持管理员删除；存在有效需求引用时必须拒绝删除并提示先迁移需求。飞书集成配置支持独立启停自动组织同步，并提供每 1/6/12/24 小时的同步频率。
2. **用户与组管理**：「用户」（账号/角色/认证源/开通默认角色）+「用户组」（专业线资源池：负责人 TM/组授予角色/成员）。
3. **角色与权限**：「角色定义」（内置 12 个可改名 + 自定义复制模板）+「预分配规则」（部门→首次开通默认角色）+「权限配置」（角色×系统模块×4 动作矩阵）。
4. **数据字典**（业务线/关闭代码/类别等下拉项 + 公司名等系统配置）。
5. **状态机配置**（每类单据的状态与流转规则可视化编辑，含在用状态删除保护）。
6. **系统集成**（飞书、SMTP、AD/LDAP 等外部连接）。
7. **审计日志**（admin/is_mgr/auditor 可查）。
8. **界面与品牌**（admin）：系统中英文名称/简介、横版与方形 Logo、Favicon、登录门户、主色/主题/密度/侧栏、角色默认首页、顶部公告及环境标识。图片选择后先进入可拖动、缩放、旋转的裁剪器；横版 Logo 使用 4:1、方形 Logo/Favicon 使用 1:1、登录背景使用 16:9，确认后仅上传裁剪结果。登录后按角色采用两套明确区分的应用壳层：仅持 `requester` 角色的业务用户使用方案 F「模块化服务门户」（顶部导航、帮助搜索、快捷入口和卡片式服务目录）；其余角色使用方案 C「精密高密度工作台」（216px 浅色紧凑侧栏、全局工作条、低圆角高信息密度画布；一、二级导航保持协调的浅色层级）。侧栏宽度以 Logo/系统名称区域及最长导航文字单行显示为约束，仅保留必要的左右留白。内部工作台展开侧栏的横版 Logo 独占一行，置于系统名称上方；横版 Logo、系统名称及英文副标题沿同一中轴线居中对齐，折叠后改用方形 Logo。侧栏菜单区在展开项超出视口时独立纵向滚动，品牌区与顶部账户入口保持可见；侧栏底部不重复展示当前用户及角色，账户入口统一位于顶部栏。配置采用“草稿 → 预览 → 发布”，保留发布历史并支持回滚。

另：SLA 策略在 ITSM-SLA 看板页维护；通知发件箱后台保留。

**流程定义管理**（流程中心）：流程/步骤全量可配置——稳定节点编码、步骤名称、节点类型（处理/审批）、默认指派（角色/用户组）、知会人、L1-L4 自治级别、步骤 SLA、触发条件（同类单据下激活流程的触发条件必须唯一）。任务生成时写入流程版本、节点编码和 RACI（主责/知会）快照；已有实例的节点、处理人、知会关系、节点类型和 SLA 锁定，通过**另存新版本**演进（老单据沿用旧版，新单据走新版）。

---

## 11. 非功能需求

| 项 | 要求 |
| --- | --- |
| 技术栈 | 后端 FastAPI + SQLAlchemy + PostgreSQL；前端 React + Ant Design；单仓库 |
| 部署 | IDC Kubernetes 为唯一交付与验收环境，使用 `deploy/k8s/push-images.sh` + `k8s-deploy.sh` 发布并做外部健康检查；Docker Compose 仅作为临时本地排障环境 |
| 性能 | 列表页 ≤ 1s；Dashboard ≤ 2s；数据规模按 5 年 10 万工单设计 |
| 安全 | 密码哈希存储、JWT 会话、接口级 RBAC、审计日志不可改 |
| API 约定 | 统一响应 `{success, data, total, page}`；RESTful；OpenAPI 文档自动生成 |
| 数据 | 每日自动备份；软删除（业务单据不物理删除） |
| 浏览器 | Chrome/Edge 最新两个版本 |

---

## 12. 里程碑映射与总验收

| 里程碑 | 覆盖 PRD 章节 | 核心验收 |
| --- | --- | --- |
| M1 骨架 | 2、3、10、11 | 登录、角色菜单、人员主数据、空 Dashboard、部署跑通 |
| M2 工单+SLA+目录 | 5.1、5.3、5.5 | 30 秒建单、变更审批流、SLA 自动判定 |
| M3 ITSM 其余 | 5.2、5.4、5.6、5.7 | CMDB 影响分析、合同预警、知识一键沉淀 |
| M4 项目 | 6 | 章程导入、甘特图、健康度自动 |
| M5 需求 | 7 | 四阶段闭环、转出问题/知识 |
| M6 团队+Dashboard+流程 | 4、8、9 | 积分自动计分与排行、人效框架、总览页、流程监控 |
| M34–35 通知与组织同步 | 2、3、10 | 开通申请通知管理员；全公司组织同步后台执行、状态可查询、完成/失败可通知 |
| M36–37 账号与个人设置 | 2.4、3 | 账号安全删除、飞书工作台免登、个人中心、自助改密、绑定管理、通知/主题/密度偏好、个人操作记录 |
| M38 界面与品牌首期 | 10、11 | 品牌与登录门户配置、受控图片上传、全局主题、角色默认首页、公告/环境标识、版本发布与回滚；公开配置异常时回退内置默认值 |
| M41 角色化视觉重构 | 10、11 | 业务用户采用方案 F 服务门户，其余角色采用方案 C 高密度工作台；横版 Logo 在展开侧栏中独占标题上方整行 |

**系统级总验收**：所有创建表单必填 ≤ 5；全系统无任何"手工维护统计数据"的页面；六类积分事件全部自动触发；一个真实业务闭环走通（业务用户报单 → 处理 → 沉淀知识；需求登记 → 分析 → 挂项目实现 → 关闭转出）。
