import React from 'react';
import ReactDOM from 'react-dom/client';
import AppRoot from './i18n/AppRoot';
import 'antd/dist/reset.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <AppRoot />
  </React.StrictMode>,
);
